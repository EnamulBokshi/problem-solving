function addBinary(a:string, b:string):string{

    if(a.length < b.length){
        const temp = a;
        a = b;
        b = temp;
    }
    let carry, result;
    for(let i = a.length; i<0; i--){
        if(a[i])
    }

    return ''
}